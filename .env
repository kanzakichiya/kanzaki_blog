# --- 这是你的配置文件 ---

# 1. 管理员账号配置
ADMIN_USER=admin
ADMIN_PASSWORD=admin

# 2. 数据库配置 (PostgreSQL)
# (这些值必须和 docker-compose.yml 里的 'db' 服务一致)
POSTGRES_USER=mybloguser
POSTGRES_PASSWORD=myblogpassword
POSTGRES_DB=myblogdb

# 3. 后端数据库连接字符串
# (格式: postgresql://USER:PASSWORD@HOST/DBNAME)
# (注意：HOST 必须是 'db'，这是 docker-compose 里的服务名)
DATABASE_URL=postgresql://mybloguser:myblogpassword@db/myblogdb