server {
    listen 80;
    server_name localhost;

    # 托管静态文件的根目录
    root /usr/share/nginx/html;
    index index.html index.htm;

    location / {
        # 关键：这个配置是为了让 Vue Router (History 模式) 正常工作
        # 任何 404 请求都重定向到 index.html
        try_files $uri $uri/ /index.html;
    }

    # (可选) 错误页面
    error_page 500 502 503 504 /50x.html;
    location = /50x.html {
        root /usr/share/nginx/html;
    }
}